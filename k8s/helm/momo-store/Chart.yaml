apiVersion: v2
name: momo-store
description: Momo Store Application (Backend + Frontend with S3 static)
type: application
version: 0.3.0
appVersion: "1.1.0"

dependencies:
  - name: ingress-nginx
    version: "4.10.0"
    repository: "https://kubernetes.github.io/ingress-nginx"
    condition: ingress-nginx.enabled